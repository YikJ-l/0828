# Golang mDNS 资产测绘 CLI 程序实现方案

## 1. 核心需求分析
- **输入**: IP 网段 (例如 `192.168.1.0/24` 或 `192.168.1.1-192.168.1.100`) 和 端口范围 (例如 `1-65535`)。
- **输出**: 目标网段内在指定端口范围内的 mDNS 协议资产信息，要求提取出 IP、端口、Host 以及基于 TXT 记录深度识别的 Banner (如设备型号、固件版本等)。
- **输出格式**: 严格对齐 README 示例中的格式，包含 `services:`、`device-info:` 和 `answers:` 结构。

## 2. 技术选型
- **开发语言**: Golang (利用 goroutine 优势实现高并发网络扫描)。
- **核心库**:
  - 协议解析：`github.com/miekg/dns` 用于构造 mDNS (DNS) 请求报文并解析复杂的响应。
  - 网络通信：Golang 标准库 `net` (UDP)。
  - 参数解析：标准库 `flag` 或字符串解析。

## 3. 架构与执行流程

### 3.1 参数解析模块
- **IP 解析**: 支持解析 CIDR 格式以及破折号范围格式，将输入的网段展开为需要扫描的 IP 列表。
- **端口解析**: 将输入的端口范围（如 `80,445,5000-6000`）解析为哈希表或区间，用于对 mDNS **发现的服务端口**进行过滤。

### 3.2 mDNS 单播探测模块 (Unicast mDNS)
- **探测原理**: mDNS 默认工作在多播 (224.0.0.251:5353)，但为了测绘**特定 IP 网段**，程序需向目标网段中的每个 IP 的 UDP `5353` 端口发送**单播 mDNS 查询**。
- **查询策略**:
  1. 向目标发送 `_services._dns-sd._udp.local.` (PTR) 请求，询问其支持的所有服务。
  2. 部分设备会在附加记录 (Additional) 中直接返回 SRV、TXT、A 记录；若不完整，则针对发现的具体服务 (如 `_http._tcp.local.`) 继续发送查询。
- **并发控制**: 使用协程池 (Goroutine Pool) + `sync.WaitGroup` 来控制扫描并发度，并设置 UDP 读写超时，防止阻塞。

### 3.3 数据解析与信息提取模块
利用 `miekg/dns` 解析响应包，提取深度 Banner：
- **PTR 记录**: 提取设备宣称支持的服务类型列表 (如 `_workstation._tcp.local`)。
- **SRV 记录**: 提取服务所对应的**真实端口 (Port)** 和 **主机名 (Hostname)**。
- **A / AAAA 记录**: 提取对应的 IPv4 和 IPv6 地址。
- **TXT 记录**: 提取服务的深度 Banner 属性，按键值对解析 (如 `model=TS-X64`, `fwVer=5.2.9`, `path=/` 等)。

### 3.4 过滤与格式化输出模块
- **数据过滤**: 将 SRV 记录解析出的真实服务端口与用户输入的端口范围进行比对，若不在范围内则剔除该服务。
- **格式化输出**: 按照要求的文本格式，将数据映射为字符串并打印：
  - `services:` 节点：输出如 `5000/tcp http:` 及其详细属性 (IP, Hostname, TTL, TXT 字段等)。
  - `device-info:` 节点：提取并输出专门针对设备的基础信息 (通常来自 `_device-info._tcp.local`)。
  - `answers:` 节点：列出所有的 PTR 响应目标。

## 4. 后续代码实现步骤规划 (TODO)
1. **项目初始化**: `go mod init mdns-scanner`，并下载依赖 `go get github.com/miekg/dns`。
2. **编写参数解析器**: 完成 IP 段和端口段的输入解析工具函数。
3. **编写 mDNS 核心探测器**: 实现构建 DNS 报文、发送 UDP 请求、接收并解析响应的核心逻辑。
4. **编写资产重组逻辑**: 将 PTR、SRV、TXT、A 记录通过服务实例名称 (Instance Name) 关联在一起，形成结构化数据。
5. **编写输出渲染器**: 严格按照示例文本格式输出最终结果。
6. **集成与测试**: 联调跑通整个流程。